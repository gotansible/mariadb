---
- hosts: all
  sudo: true
  roles:
    - role: mariadb
      mariadb_root_password: abc123def
      mariadb_databases:
        - name: wordpress
      mariadb_users:
        - name: wpuser
          password: some123pass
          priv: 'wordpress.*:ALL'
